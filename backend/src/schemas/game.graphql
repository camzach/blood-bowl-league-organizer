type Game {
  homeTeam: Team!
  awayTeam: Team!
  ffHome: Int!
  ffAway: Int!
  tdHome: Int!
  tdAway: Int!
  casHome: Int!
  casAway: Int!
  winningsHome: Int!
  winningsAway: Int!
}

extend type Query {
  games: [Game!]!
  game(home: String! away: String!): Game
}
