type Schedule @entity {
  rounds: [Round!]! @column
}

type Round {
  games: [ScheduleSlot!]! @column
}

type ScheduleSlot {
  homeTeam: Team! @link
  awayTeam: Team! @link
  game: Game @link
}

extend type Query {
  schedule: Schedule!
}
