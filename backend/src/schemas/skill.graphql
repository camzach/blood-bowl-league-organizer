type Skill @entity(additionalFields: [
  {path: "purchasable", type: "boolean"}
]) {
  id: String! @id
  name: String! @column
  rules: String! @column
  category: SkillCategory! @column
}

enum SkillCategory {
  G
  A
  M
  P
  S
  T
}

extend type Query {
  skills: [Skill!]!
}
