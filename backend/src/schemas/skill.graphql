type Skill @entity {
  id: String! @id
  name: String! @column
  rules: String! @column
  category: SkillCategory! @column(overrideType: "'G' | 'A' | 'M' | 'P' | 'S' | 'T'")
}

enum SkillCategory {
  G
  A
  M
  P
  S
  T
}

extend type Query {
  skills: [Skill!]!
}
