query PregameInfo(
  $home: String!
  $away: String!
) {
  home: team(id: $home) {
    ...Pregame
  }
  away: team(id: $away) {
    ...Pregame
  }
}

fragment Pregame on Team {
  name
  id
  race {
    name
    specialRules
  }
  teamValue {
    current
  }
  players(missNextGame: false) {
    ...TeamTablePlayer
  }
  fans
  treasury
}